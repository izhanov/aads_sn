<h1><%= current_my_user.name %></h1>

<div class="container">
  <div class="row">
    <div class="col-2">
      <nav class="nav flex-column">
        <%= link_to "My Posts", "#", class: "nav-link" %>
        <%= link_to "My Followers", "#", class: "nav-link" %>
        <%= link_to "My Following", "#", class: "nav-link" %>
      </nav>
    </div>
    <div class="col-10">
      <div class="row">
        <div class="col-6">
          <%= turbo_frame_tag "posts" do %>
            <%= render @posts %>
          <% end %>
        </div>
        <div class="col-6">
          <div class="d-flex justify-content-end">
            <%= link_to "New Post", new_my_post_path, class: "btn btn-primary", data: { turbo_frame: dom_id(Post.new) } %>
          </div>

          <%= turbo_frame_tag Post.new %>
        </div>
      </div>
    </div>
  </div>
</div>
